/**
 * /console-demo — the overview, on the sample class. The real overview page
 * reads everything from the provider the demo layout supplies, so it is
 * re-exported rather than re-written.
 */
export { default } from '@/app/console/class/[id]/page';
