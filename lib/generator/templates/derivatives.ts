import type { GenTemplate } from '../types';

// Deliberately empty: the owner asked to leave חשבון דיפרנציאלי without generator templates for now (2026-09-04).
export const DERIVATIVES_TEMPLATES: GenTemplate[] = [];
